# plugins (tpm)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'azorng/tmux-smooth-scroll'

# session persistence
set -g @resurrect-save 'S'                      # remap off C-s (prefix) to keep send-prefix
set -g @resurrect-restore 'R'
set -g @resurrect-strategy-nvim 'session'       # restore nvim sessions
set -g @resurrect-capture-pane-contents 'on'    # restore pane scrollback
set -g @continuum-restore 'on'                  # auto-restore on tmux server start

# session switcher (sessionx)
set -g @sessionx-bind 's'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-preview-location 'right'
set -g @sessionx-preview-ratio '55%'
# unify look with the other fzf pickers (Telescope-style)
set -g @sessionx-fzf-builtin-tmux 'on'        # let fzf draw the popup itself
set -g @sessionx-window-width '80%'
set -g @sessionx-window-height '90%'
# sessionx runs fzf via `tmux run-shell`, which only sees the tmux server's
# (often stale) inherited FZF_DEFAULT_OPTS -- so the colorscheme must be passed
# explicitly here. Geometry/behavior flags go on the option directly; the --color
# palette is appended from the SINGLE SOURCE shared with zsh/zshrc
# (themes/tokyonight-moon/fzf/tokyonight_moon.conf) so the two never drift. The
# run-shell tr's that file's one-flag-per-line into a single appended string; all
# of this lands on fzf's command line and overrides the stale FZF_DEFAULT_OPTS.
set -g @sessionx-additional-options '--ansi --info=inline-right --highlight-line --layout=default --border=none --list-border=rounded --input-border=rounded --preview-border=rounded --gutter=" "'
run-shell 'tmux set -ga @sessionx-additional-options " $(tr "\n" " " < ~/dotfiles/themes/tokyonight-moon/fzf/tokyonight_moon.conf)"'
set -g @sessionx-pointer '>'
set -g @sessionx-prompt '> '

# smooth scrolling
set -g @smooth-scroll-speed "100"
set -g @smooth-scroll-easing "sine"
set -g @smooth-scroll-normal "3"
set -g @smooth-scroll-halfpage ""
set -g @smooth-scroll-fullpage ""
set -g @smooth-scroll-mouse "true"
set -g @smooth-scroll-exit-copy-mode-at-bottom "true"

# initialize tpm (keep this line at the very bottom)
run '~/.config/tmux/plugins/tpm/tpm'
